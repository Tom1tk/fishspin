# Required environment variables for wheel-app.
# Copy to .env and fill in real values before starting the server.

# Flask session secret key.
# Generate a new one with: python -c "import secrets; print(secrets.token_hex(32))"
WHEEL_SECRET_KEY=

# PostgreSQL connection string.
DATABASE_URL=postgresql://wheelapp:password@localhost/wheeldb
